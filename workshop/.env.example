# Copy to `.env` and fill in. Used by `main.py` load_dotenv() for GLM (智谱) via OpenAI-compatible API.
# See workshop/config.yaml — default model `glm-4-plus` uses this key with base_url open.bigmodel.cn
OPENAI_API_KEY=

# Optional: bind port (default 8000)
# PORT=8000
