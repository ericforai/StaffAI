# Manual QA — Delivery Lighthouse（对照 `spec.md` §3）

**日期：** 2026-04-10  
**范围：** Happy path（S0→S8）、两种失败恢复、与实现/自动化对齐说明  
**签署：** 交付负责人在「手测结果」列填 ✅ / ❌ 并署名后，可将 E2 标为完全闭环。

---

## 环境

- 后端：`cd hq/backend && npm run dev:web`（默认 `http://localhost:3333`）
- 前端：`cd hq/frontend && npm run build && npx next start --hostname 127.0.0.1 --port 3010`（与 Playwright 一致；或本地约定端口）
- 自动化回归：`cd hq/frontend && CI=1 npm run test:e2e`（应 18/18，含 S0 模式切换恢复草稿与 intent 创建失败重试）

---

## §3 阶段走查表

| 阶段 | 手测步骤（摘要） | 预期完成态 | 失败恢复（spec） | 手测结果 | 备注 / 缺口 |
|------|------------------|------------|------------------|----------|-------------|
| **S0** | `/tasks` 在「简易 / 高级向导」间切换 | 能进入两种创建方式 | 切换模式 |  | **工程**：`staffai-intent-draft-id` + URL `intentId` 恢复（DL-01）；手测仍建议真人勾选 |
| **S1** | 高级向导：输入需求 → 开始需求对齐 | 进入澄清 UI，`intent` 已创建 | 错误区 **重试创建需求** |  | **E2E**：mock `POST /api/intents` + 失败重试（DL-04）；完整 LLM 链仍需手测 |
| **S2** | 澄清若干轮直至进入设计确认 | 自动/手动进入 Step 2 | **重试上次澄清** |  | 无「跳过澄清」为刻意不做（spec §8） |
| **S3** | 确认设计方案 | 进入计划预览（Step 3） | **重试确认与生成计划** |  |  |
| **S4** | 预览计划 → 创建任务 | 跳转到任务详情或列表 | **重试创建任务** |  |  |
| **S5** | 简易模式：选人 + 创建任务 | 任务出现在列表，可选稍后执行 | 改参数重建 |  | E2E 已覆盖创建流 |
| **S6** | 执行任务；高风险任务走审批 | 执行终态或 `waiting_approval`；可去审批 | 审批通过后续跑；失败可再看详情 |  | Hero 在 **简易模式** 下有待批时优先审批；任务页 **横幅** + 审批队列 |
| **S7** | 任务详情 →「交付产物」Tab | 有 assignments 产物时可见正文；否则空态 | 刷新 / 重跑执行 |  | **工程**：正文 + 复制/导出 Markdown（DL-02）；独立文件下载/外链可后续做 |
| **S8** | 「保存为模板」 | 成功提示或模板中心可见 | 失败后 **同名校验重试** |  | **工程**：模态框 + 内联成功 + 模板中心链（DL-03） |

---

## Happy path（整故事 smoke）

建议在**真实后端 + LLM 可用**环境下由产品/交付走一次端到端（自动化无法替代）：

1. `/tasks` → 高级向导 → 输入 → 澄清 → 确认设计 → 计划 → 创建任务。  
2. 任务详情 → 执行（若策略允许）→ 执行摘要可见。  
3. 「交付产物」Tab 打开（有数据时核对展示）。  
4. 「保存为模板」→ 模板中心检索（若已接模板 API）。

**工程侧已覆盖的片段：** Playwright `tasks-workspace`、`runtime-foundation` 等对列表、创建、执行、审批、错误条等有回归；**不替代**完整 intent→LLM 链路的 Happy path。

---

## 两种失败恢复（spec 要求 + 验证方式）

| # | 场景 | 验证方式 | 自动化 |
|---|------|----------|--------|
| **F1** | 任务列表 API 失败 | 手测：断网或 mock 500 → 出现「任务加载失败」+ **重试加载** | ✅ `tasks-workspace.spec.ts` |
| **F2** | 向导 API 失败（创建 intent / 澄清 / 确认 / 建任务） | 手测：人为断 API 或返回 5xx → `intent-wizard-error` + 对应 **重试** | ✅ 创建 intent 失败重试（DL-04）；其余步骤仍以手测为主 |

可选第三项（与 S6 一致）：**待审批阻塞** — Hero 文案 + **前往审批** + 任务详情 **delivery-approval-banner**；Playwright 已部分覆盖。

---

## 结论（工程预检）

- **与 spec 已知差异**已记入 `github-issues-draft.md`，便于一键建 Issue。  
- **E2 手测签署**：上表「手测结果」列需真人勾选后，方可视为产品级签收。

---

## 实施对照（工程闭环 vs 手测表）

| 手测 / Issue | 工程状态 |
|----------------|----------|
| S0 / DL-01 | 已实现：`staffai-intent-draft-id` + URL `intentId` 恢复；建任务成功清除。**E2E**：简易↔高级后回到澄清步。 |
| S1 / F2 / DL-04 | 已实现：E2E 覆盖「创建 intent 500 → 重试 → 澄清页」。 |
| S7 / DL-02 | 已对齐 spec：`ArtifactsPanel` 正文 + 复制/导出 Markdown。 |
| S8 / DL-03 | 已实现：模态框 `save-template-modal` + 内联成功 + `/templates` 链接。 |
| 「手测结果」列 | **仍空**：需真人在上表逐行 ✅/❌，不可替代。 |
